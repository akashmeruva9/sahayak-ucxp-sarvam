fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
itsdangerous>=2.2
# FastAPI raises at import -- not at request time -- as soon as a route declares
# UploadFile, so without this the whole app fails to boot and the healthcheck
# never answers. /api/voice-onboard takes the merchant's recording as a file.
# Floor is 0.0.18: earlier versions carry CVE-2024-53981.
python-multipart>=0.0.18
